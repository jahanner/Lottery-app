// import { addReducer } from "redux-easy";
//
// addReducer("setLotteryDate", (state, lotteryDate) => {
//   const { lotteryApp } = state;
//   return { ...state, date: { ...lotteryApp, lotteryDate } };
// });
//
// addReducer("setPrizeDescription", (state, prizeDescription) => {
//   const { lotteryApp } = state;
//   return { ...state, { ...lotteryApp, prizeDescription } };
// });
//
// addReducer("setWinnerMessage", (state, winnerMessage) => {
//   const { lotteryApp } = state;
//   return { ...state, Message: { ...lotteryApp, winnerMessage } };
// });

// addReducer('addEntry', (state, numberOfEntries) => {
//   const {lotteryApp} = state;
//   return {...state, Entries: {...lotteryApp, lotteryApp.numberOfEntries + 1}
// });

// addReducer("setError", (state, error) => {
//   const { lotteryApp } = state;
//   return { ...state, Error: { lotteryApp, error } };
// });
